name: Testing Self Hosted Runner

on: pull_request

jobs:
  hello:
    runs-on: KALI-LINUX
    steps:
      - name: Say Hello
        run: echo "Hello"
      - name: Print Secret
        run: echo ${{ secrets.PASSWORD }}
